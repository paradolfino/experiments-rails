<h3><%= @user.username %></h3>
<% @msgs.each do |msg| %>
    <div class="post">
        <div class="post-title">
            <h5>[<%= truncate(msg.title, length: 15) %>]</h5>
        </div>
        <div class="post-body">
            <p><%= truncate(msg.body, length: 100) %></p>
        </div>
        <div class="post-meta">
            <small><em>Created <strong><%= time_ago_in_words(post.created_at) %></strong> ago.</em></small>
        </div>
    </div>
<% end %>