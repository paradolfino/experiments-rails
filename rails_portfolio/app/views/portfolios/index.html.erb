<% @portfolios.each do |item| %>

    <h1><%= item.title %></h1>
    <h2><%= item.subtitle %></h2>
    <p><%= item.body %></p>
    <%= image_tag item.thumb_image if item.thumb_image %>
    <%= link_to "Edit" %>

<% end %>