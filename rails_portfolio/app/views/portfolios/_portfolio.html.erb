<h1><%= link_to portfolio.title, portfolio_show_path(portfolio) %> [<%= link_to "Edit", edit_portfolio_path(portfolio) %>] [<%= link_to "Destroy", portfolio_path(portfolio), method: :delete, data: {confirm: "Are you sure!?"}  %>]</h1>
<h2><%= portfolio.subtitle %></h2>
<p><%= portfolio.body %></p>
<%= image_tag portfolio.thumb_image if portfolio.thumb_image %>