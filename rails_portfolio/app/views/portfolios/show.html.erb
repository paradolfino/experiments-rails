<h2><%= @portfolio.title %></h2>
<h4><%= @portfolio.subtitle if @portfolio.subtitle %></h4>
<p><%= @portfolio.body %></p>
<%= image_tag @portfolio.main_image if @portfolio.main_image %><br />
<%= link_to "Back to All Portfolios", portfolios_path %><br />
<%= if @portfolio.technologies then @portfolio.technologies.each do |tech| %>

<% end %>